menu "G6 Brain Configuration"

    config G6_RLS_LAMBDA
        float "RLS forgetting factor"
        default 0.98
        range 0.9 0.999

    config G6_MAX_TEMP_C
        int "Max ASIC temperature (C)"
        default 70

    config G6_MAX_VOLTAGE_MV
        int "Max voltage (mV)"
        default 1350

    config G6_PUZZLE_EXTRA_ENABLE
        bool "Enable puzzle nonce optimization"
        default y

endmenu
