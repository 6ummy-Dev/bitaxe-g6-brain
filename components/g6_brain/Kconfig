menu "G6 Brain Configuration (v1.0 Beta QA Hardened)"

    config G6_RLS_LAMBDA
        float "RLS forgetting factor"
        default 0.98
        range 0.9 0.999

    config G6_TEMP_CEILING
        int "Smart DFS thermal ceiling (C)"
        default 70

    config G6_DFS_STEP
        int "DFS frequency step (MHz)"
        default 25

    config G6_NER_THRESHOLD
        float "P-VUS NER threshold"
        default 0.001

    config G6_I2C_HARD_FAULT_THRESHOLD
        int "I2C hard fault threshold (consecutive timeouts)"
        default 5
        range 3 20

    # QA HARDENING KNOBS
    config G6_PROACTIVE_DFS_THRESHOLD
        float "Proactive ΔT/dt threshold (°C/s)"
        default 2.0

    config G6_VOLTAGE_RIPPLE_MAX
        float "Max allowed voltage ripple (%)"
        default 5.0

    config G6_NVS_WRITE_INTERVAL
        int "NVS write interval (ticks) — wear leveling"
        default 30000

    config G6_KP
        float "PID Kp"
        default 2.0

    config G6_KI
        float "PID Ki"
        default 0.1

    config G6_KD
        float "PID Kd"
        default 0.5

    # STOCHASTIC NONCE FEATURES
    config G6_LOW_LATENCY_JOBS
        bool "Enable low-latency job mode (double-buffering hook)"
        default y

endmenu
